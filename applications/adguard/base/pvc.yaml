apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-data
spec:
  # Shared conf/work across replicas (hostNetwork, one pod per node).
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
